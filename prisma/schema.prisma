

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model Urlmap{
  userUrl String @unique @id
  realUrl String
}

model Urlcount{
  userUrl String @unique @id
  count Int @default(1)
}